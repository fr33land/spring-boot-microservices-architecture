---
debug: false
server:
    port: 8888
    
spring:
    application:
        name: config-server
    profiles:
        active: @spring.profiles.active@
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/fr33land/ms-config-repo
                    cloneOnStart: true