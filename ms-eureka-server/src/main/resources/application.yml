---
debug: false
server:
    port: 8761
    
spring:
    application:
        name: eureka-server
    cloud:
        config:
            uri: http://localhost:8888   
    profiles:
        active: @spring.profiles.active@