---
debug: false
server:
    port: 8761
    
spring:
    application:
        name: eureka-server
    profiles:
        active: @spring.profiles.active@
    cloud:
        config:
            uri: @spring.config.server@